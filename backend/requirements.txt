# Grupo RYSA ERP - Dependencias de backend (runtime + tests)
# Eliminada la cadena de dependencias de la plataforma Emergent (litellm, openai,
# google-*, boto3, s3, stripe, etc.) que no se usa en el código actual.

# --- Runtime ---
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.13.4
email-validator==2.3.0
python-multipart==0.0.32
bcrypt==4.1.3
PyJWT==2.13.0
python-dotenv==1.2.2
httpx==0.28.1
requests==2.34.2
pandas==3.0.5
openpyxl==3.1.5
xlrd==2.0.2
xlwt==1.3.0
reportlab==5.0.0
qrcode>=8.0
Pillow>=10.4.0

# --- PostgreSQL (única base de datos soportada) ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.13

# --- Tests ---
pytest==9.1.1
pytest-xdist==3.8.0
pytest-asyncio==1.4.0

